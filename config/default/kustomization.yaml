namespace: pool-lifecycle-controller-system

namePrefix: pool-lifecycle-

resources:
- ../rbac
- ../manager
- metrics_service.yaml

patches:
- path: manager_metrics_patch.yaml
  target:
    kind: Deployment
